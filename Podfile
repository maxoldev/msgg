# Uncomment the next line to define a global platform for your project
platform :tvos, '11.0'

target 'MSGG' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # use_modular_headers!

  # Pods for MSGG
  pod 'SDWebImage', '~> 5.0', :modular_headers => true
  pod 'MarqueeLabel', '~> 4.0'
  pod 'Swinject', '~> 2.6.0', :modular_headers => true
end

target 'TopShelf' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # use_modular_headers!
  
  # Pods for MSGG
  pod 'Swinject', '~> 2.6.0', :modular_headers => true
end
